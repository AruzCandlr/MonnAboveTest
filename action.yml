name: "JaoMonnBot"
description: "Comment on a newly opened issue with an OpenAI generated response."
author: "AruzCandlr"
inputs:
  openai_api_key:
    description: "OpenAI API key (pass in via secrets)"
    required: true
  github_token:
    description: "GitHub token (pass in via secrets, default GITHUB_TOKEN)"
    required: true
runs:
  using: "composite"
  steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: Install deps
      run: |
        python -m pip install --upgrade pip
        pip install pygithub openai

    - name: Run bot
      env:
        GITHUB_TOKEN: ${{ inputs.github_token }}
        OPENAI_API_KEY: ${{ inputs.openai_api_key }}
        # ISSUE_NUMBER and GITHUB_REPOSITORY are expected to be supplied by the caller workflow
        ISSUE_NUMBER: ${{ inputs.issue_number || '' }}
        GITHUB_REPOSITORY: ${{ inputs.repo || '' }}
      run: |
        python bot.py

branding:
  icon: "activity"
  color: "blue"
